/**
 * What the seller makes. Chosen at registration, editable later in the
 * profile, and shown on the public artist page.
 */
export enum SellerType {
  Artist = 'artist', // მხატვარი
  Handmade = 'handmade', // ხელნაკეთი ნივთები
  Both = 'both', // ორივე
}
